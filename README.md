# Student Management System

## Project Title
Student Management System (ProjectName_RollNumber: Student-Management_68)

## Problem Statement
Managing student records manually is time-consuming and error-prone. This project aims to build a graphical user interface (GUI) application to efficiently add, update, delete, search, and view student details such as roll number, name, course, marks, and attendance.

## Tools Used
- **Language**: Python 3
- **GUI Framework**: Tkinter (Standard Library)
- **Database**: SQLite3 (Standard Library)

## Installation Steps
1. Clone the repository:
   ```bash
   git clone https://github.com/NiyafHussainC/Student-Management.git
   ```
2. Navigate to the project directory:
   ```bash
   cd Student-Management
   ```
3. No external dependencies via `pip` are required. The project exclusively uses Python's standard libraries.

## Execution Procedure
1. Run the main application script:
   ```bash
   python main.py
   ```
2. The application will automatically create an SQLite database file (`students.db`) if it does not already exist.

## Output Screenshots
*(Note: Insert screenshots of the repository homepage, commit history, and the functioning application GUI here in your final report/before submission)*
- **Repository Homepage**: [Insert Screenshot]
- **Commit History**: [Insert Screenshot]
- **Application GUI**: [Insert Screenshot]

## Conclusion
This Student Management System successfully demonstrates the integration of a Python Tkinter frontend with an SQLite database backend. It fulfills all the basic CRUD (Create, Read, Update, Delete) requirements for managing student data efficiently.
